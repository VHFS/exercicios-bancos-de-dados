#include <stdio.h>
int main( ) {

 int x, *p; 
 x=10; 
 // Só se usa asterisco quando se está declaando o ponteiro, deve-se usar & para impimir valor contido em alguma variavel
 p = &x; 
 return 0 ;
 }
 
